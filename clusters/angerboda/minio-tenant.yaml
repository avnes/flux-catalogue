---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: minio-tenant
  namespace: flux-system
spec:
  dependsOn:
    - name: longhorn
    - name: minio-operator
  interval: 1m0s
  sourceRef:
    kind: GitRepository
    name: catalogue
  path: "./infrastructure/minio-tenant"
  prune: true
  validation: client
  patches:
    - patch: |-
        apiVersion: helm.toolkit.fluxcd.io/v2beta1
        kind: HelmRelease
        metadata:
          name: minio-tenant
          namespace: minio-tenant
        spec:
          values:
            ingress:
              console:
                enabled: true
                host: minio-tenant-console.angerboda.anyops.site
                tls: []
                annotations:
                  external-dns.alpha.kubernetes.io/target: traefik.angerboda.anyops.site
      target:
        kind: HelmRelease
        name: minio-tenant
        namespace: minio-tenant
