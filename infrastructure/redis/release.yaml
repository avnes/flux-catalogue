---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: redis
  namespace: redis
spec:
  chart:
    spec:
      chart: redis
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  interval: 1m0s
  install:
    remediation:
      retries: 3
  values:
    master:
      service:
        type: LoadBalancer
        annotations:
          external-dns.alpha.kubernetes.io/hostname: "redis-master.anyops.site"
    replica:
      service:
        type: LoadBalancer
        external-dns.alpha.kubernetes.io/hostname: "redis.anyops.site"
    auth:
      existingSecret: redis-auth
